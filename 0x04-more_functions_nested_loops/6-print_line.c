#include "main.h"

/**
 * print_line - prints a line of length n
 * @n: any given number
 * Return: A line of underscores
 */

void print_line(int n)
{
	int a;

	if (n <= 0)
		_putchar('\n');
	else if
	{
		for (a = 0; a < n; a++)
		{
			_putchar('_');
		}
		_putchar('\n');
	}
}
